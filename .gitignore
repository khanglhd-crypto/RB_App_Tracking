# Secrets — không bao giờ commit
backend/.env
.env

# Python
__pycache__/
*.pyc
backend/**/__pycache__/

# Editor / tooling
.claude/
.vscode/

# Local scratch (không dùng tới trong repo)
uploads/
docs/

# OS
.DS_Store
Thumbs.db

# Desktop app (Electron) — chỉ commit source, không commit build/deps
desktop/node_modules/
desktop/dist/
desktop/backend-dist/
desktop/build-tmp/
desktop/test_data/

# Dữ liệu file-based (mặc định khi chạy dev không set DATA_ROOT/ROOT_PATH)
backend/local_data/
backend/local_data_test/
